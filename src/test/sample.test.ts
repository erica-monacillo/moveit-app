describe("Basic Tests", () => {

  test("Addition works", () => {
    expect(2 + 3).toBe(5);
  });

  test("Subtraction works", () => {
    expect(5 - 2).toBe(3);
  });

  test("Multiplication works", () => {
    expect(3 * 3).toBe(9);
  });

  test("Division works", () => {
    expect(10 / 2).toBe(5);
  });

});
